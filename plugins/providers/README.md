# AgentBoard Provider Plugins

Target home for AI provider runtimes.

Provider packages implement AgentBoard's provider runtime contract. Existing runtime code remains inside `plugins/claude-code/agent-board-core/src` until the provider contract and fake provider are introduced.
